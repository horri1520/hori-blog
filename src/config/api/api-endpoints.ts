import API_BASE_URL from './api-base-url';

const apiEndpoints = {
  posts: `${API_BASE_URL}/posts`,
  qiitaPosts: `${API_BASE_URL}/qiita_posts`,
  category: `${API_BASE_URL}/categories`,
};

export default apiEndpoints;
